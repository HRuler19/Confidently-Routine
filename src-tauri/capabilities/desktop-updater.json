{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "Update-check permissions, desktop only - Android/iOS use their store's own update mechanism instead",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": ["updater:default", "process:default"]
}
