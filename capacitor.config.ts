import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.millihasap.confidentlyroutine',
  appName: 'Confidently Routine',
  webDir: 'www'
};

export default config;
